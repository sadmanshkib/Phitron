#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n;
        cin >> n;
        int a[n], b[n];
        int asum = 0;

        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
            asum = asum + a[i];
        }
        for (int i = 0; i < n; i++)
        {
            cin >> b[i];
        }

        
       

    }

    return 0;
}